# Database Configuration (your existing PostgreSQL setup)
DB_PASSWORD=ostrich2509
DB_USERNAME=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_SCHEMA=logging-platform

# Flyway uses the same database credentials
flyway.url=jdbc:postgresql://localhost:5432/postgres
flyway.user=postgres
flyway.password=ostrich2509
flyway.schemas=logging-platform

# Required for OAuth2 (you need to get these from Google Console)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Required security secret (generate a strong one)
JWT_SECRET=your_jwt_secret_minimum_256_bits

# Environment
SPRING_PROFILES_ACTIVE=dev
